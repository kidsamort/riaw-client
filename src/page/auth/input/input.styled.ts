import styled, { css } from 'styled-components';
import { AuthInputProps } from './input.props';

const styles = css<AuthInputProps>``;

const Input = styled.form`
	display: grid;
	gap: 12px;
`;

export default { Input };
