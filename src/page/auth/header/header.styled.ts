import styled, { css } from 'styled-components';
import { HeaderProps } from './header.props';

const styles = css<HeaderProps>``;

export const WrapperStyled = styled.div<HeaderProps>`
  ${styles}
`;
export const SpanStyled = styled.div<HeaderProps>`
  ${styles}
`;
